// scripts/not-yet.mjs
//
// Placeholder for npm verbs that arrive in Phase 2 (the Worker). Keeps the
// documented commands stable so the README never has to change.

const verb = process.argv[2] ?? "this command";
console.log(`"npm run ${verb}" arrives in Phase 2 (the Cloudflare Worker). Nothing to do yet.`);
