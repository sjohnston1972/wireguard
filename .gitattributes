# Scripts that run on Linux (the VM, the home container) must keep Unix line
# endings even when checked out on Windows, or /bin/sh fails on the CR.
*.sh text eol=lf
Dockerfile text eol=lf
*.service text eol=lf
*.timer text eol=lf
*.tftpl text eol=lf
